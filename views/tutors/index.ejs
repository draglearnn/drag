<!-- views/tutors/index.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>All Tutors</title>
  <style>
    /* styles/style.css */
    body {
      zoom: 90%;
      font-family: 'Arial', sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f5f5f5;
    }
  
    .card-container {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
      gap: 16px;
      margin-left: 4rem;
      max-width: 1200px;
      margin: 0 auto;
      padding-bottom: 1rem;
    }
  
    .tutor-card {
      background: #45a049;
      color: #333;
      display: flex;
      flex-direction: column;
      border-radius: 23px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1), 0 8px 16px rgba(0, 0, 0, 0.2);
      width: 100%;
      overflow: hidden;
      transition: transform 0.2s ease-in-out;
    }
  
    .tutor-card:hover {
      transform: scale(1.05);
    }
  
    .card-image {
      width: 100%;
      height: 60%;
      overflow: hidden;
      border-radius: 23px 23px 0 0;
    }
  
    .card-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
  
    .card-details {
      padding: 1rem;
      font-size: 0.9rem;
    }
  
    .card-details h2,
    .card-details p,
    .card-details a {
      color: black;
      font-weight: bolder;
    }
  
    .card-details a {
      text-decoration: none;
      background: black;
      padding: 0.5rem 1rem;
      border-radius: 5px;
      display: inline-block;
      color: white !important;
      width: 80%;
      text-align: center;
    }
  
    .card-details a:hover {
      background: #333;
    }
  
    h1 {
      letter-spacing: 8px;
      font-size: 4rem;
      margin-top: 1rem;
      text-align: center;
      font-weight: bolder;
    }
  
    @media screen and (max-width: 768px) {
  .card-container {
    grid-template-columns: 1fr;
    margin-left: 2rem;
    margin-right: 2rem;
    gap: 12px;
  }

  .tutor-card {
    width: calc(100% - 4rem); /* Adjust the width based on your preference */
  }

  /* .card-container {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
    gap: 16px;
    margin-left: 4rem;
    max-width: 1200px;
    margin: 0 auto;
    padding-bottom: 1rem;
    position: relative;
    left: 60px;
} */
}


  </style>
  
</head>
<body>
  <%- include('../partials/navigation.ejs') %>

  <div class="tutor-list">
    <h1>PICK FROM BEST OF BEST</h1>
    <div class="card-container">
      <% tutors.forEach(tutor => { %>
        <div class="tutor-card">
          <div class="card-image">
            <!-- Display the last image in the array -->
            <img src="<%= tutor.image && tutor.image.length ? tutor.image[tutor.image.length - 1].url : '/default-image.jpg' %>" alt="<%= tutor.name %>'s Image">
          </div>
          <div class="card-details">
            <h2><%= tutor.name %></h2>
            <p>Subject: <%= tutor.subject %></p>
            <p>Price: R<%= tutor.price || 400 %></p>
            <a href="/tutors/<%= tutor._id %>">Details</a>
          </div>
        </div>
      <% }); %>
    </div>
  </div>

  <%- include('../partials/footer.ejs') %>
</body>
</html>
